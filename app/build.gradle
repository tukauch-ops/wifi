plugins {
    id 'com.android.application'
}

android {
    namespace "com.example.ttsapp"
    compileSdk 34

    defaultConfig {
        applicationId "com.example.ttsapp"
        minSdk 21
        targetSdk 34
        versionCode 5   // 前回より必ず +1
        versionName "1.4"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
}
